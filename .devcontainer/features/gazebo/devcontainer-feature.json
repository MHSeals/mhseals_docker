{
  "id": "gazebo",
  "version": "1.0.0",
  "name": "Gazebo Installer",
  "options": {
    "variant": {
      "type": "string",
      "enum": ["classic", "garden", "none"],
      "default": "none",
      "description": "Select Gazebo version to install"
    }
  }
}